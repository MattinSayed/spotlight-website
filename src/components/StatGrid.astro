---
const { stats = [] } = Astro.props;
---

<dl class="stat-grid">
  {stats.map((stat) => (
    <div class="stat-card">
      <dt>{stat.value}</dt>
      <dd>{stat.label}</dd>
    </div>
  ))}
</dl>
